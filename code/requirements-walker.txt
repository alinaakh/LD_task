# Walker RL training only (separate virtualenv so JAX's CUDA libs never touch PyTorch's).
# Tested combination; newer JAX (>=0.10) removed an API that brax 0.14.2 still calls.
jax[cuda12]==0.9.2
brax==0.14.2
playground==0.2.0
mujoco==3.14.0
mujoco-mjx==3.14.0
flax==0.12.8
orbax-checkpoint==0.12.5
warp-lang==1.17.0
